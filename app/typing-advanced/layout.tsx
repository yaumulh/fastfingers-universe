import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Typing Advanced",
  description: "Take the advanced typing challenge with harder word flow and competitive tracking.",
};

export default function TypingAdvancedLayout({ children }: { children: React.ReactNode }) {
  return children;
}

